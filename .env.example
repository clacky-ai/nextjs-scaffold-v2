# WebSocket 配置
WS_PORT=3000
CORS_ORIGIN=http://localhost:5173

# JWT 密钥（用于 WebSocket 认证）
JWT_SECRET=your-websocket-jwt-secret-key-here

# 数据库连接（如果还没有的话）
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
