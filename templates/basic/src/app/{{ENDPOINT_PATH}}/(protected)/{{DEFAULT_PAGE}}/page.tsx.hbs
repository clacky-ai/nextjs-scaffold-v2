'use client'

import { use{{pascalCase name}}Auth } from '@/hooks/{{kebabCase name}}/use-{{kebabCase name}}-auth'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

export default function {{pascalCase name}}{{pascalCase defaultPage}}Page() {
  const { user, signOut } = use{{pascalCase name}}Auth()

  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold tracking-tight">
          {{#if (eq defaultPage 'dashboard')}}
          {{displayName}} Dashboard
          {{else if (eq defaultPage 'account')}}
          My Account
          {{else}}
          {{pascalCase defaultPage}}
          {{/if}}
        </h1>
        <p className="text-gray-600">
          {{#if (eq defaultPage 'dashboard')}}
          Welcome to your {{displayName}} dashboard
          {{else if (eq defaultPage 'account')}}
          Manage your account settings and preferences
          {{else}}
          {{displayName}} {{defaultPage}} page
          {{/if}}
        </p>
      </div>

      {{#if user}}
      <Card>
        <CardHeader>
          <CardTitle>Welcome back!</CardTitle>
          <CardDescription>
            You are signed in as {user.email || user.name}
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div className="space-y-4">
            <p className="text-sm text-gray-600">
              {{#if roles.length}}
              Role: {user.role || 'User'}
              {{/if}}
            </p>
            
            {{#if (eq defaultPage 'dashboard')}}
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
              {{#if (eq name 'customer')}}
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Orders</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-2xl font-bold">0</p>
                  <p className="text-sm text-gray-600">Total orders</p>
                </CardContent>
              </Card>
              
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Favorites</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-2xl font-bold">0</p>
                  <p className="text-sm text-gray-600">Favorite items</p>
                </CardContent>
              </Card>
              {{else if (eq name 'merchant')}}
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Sales</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-2xl font-bold">$0</p>
                  <p className="text-sm text-gray-600">This month</p>
                </CardContent>
              </Card>
              
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Orders</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-2xl font-bold">0</p>
                  <p className="text-sm text-gray-600">Pending orders</p>
                </CardContent>
              </Card>
              
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Products</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-2xl font-bold">0</p>
                  <p className="text-sm text-gray-600">Active products</p>
                </CardContent>
              </Card>
              {{else}}
              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Overview</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-sm text-gray-600">Dashboard content for {{displayName}}</p>
                </CardContent>
              </Card>
              {{/if}}
            </div>
            {{/if}}
            
            <Button 
              variant="outline" 
              onClick={() => signOut()}
              className="w-full sm:w-auto"
            >
              Sign Out
            </Button>
          </div>
        </CardContent>
      </Card>
      {{/if}}
    </div>
  )
}
